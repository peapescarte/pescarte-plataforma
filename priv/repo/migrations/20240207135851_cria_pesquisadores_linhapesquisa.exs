defmodule Pescarte.Database.Repo.Migrations.CriaPesquisadoresLinhapesquisa do
  use Ecto.Migration

  def change do

    # create table(:pesquisador_lp, primary_key: false) do
    #   add :pesquisador, references(:pesquisador, on_delete: :delete_all), primary_key: true
    #   add :linha_pesquisa, references(:linha_pesquisa, on_delete: :delete_all), primary_key: true
    #   add :lider?, :boolean
    #   timestamps()
    # end
    # create(index(:pesquisador_lp, [:pesquisador]))
    # create(index(:pesquisador_lp, [:linha_pesquisa]))




  end
end
